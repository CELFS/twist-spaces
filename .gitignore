# Local requirements are maintained by the user.
/temp/

# Swift build products, caches, and local app bundles.
/.build/
/build/
/.swiftpm/
# Machine-specific certificate fingerprint; the private key stays in the login keychain.
/signing.local.json
.DS_Store
*.xcuserstate
xcuserdata/
